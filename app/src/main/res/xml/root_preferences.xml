<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ListPreference
        android:key="pref_speed_unit"
        android:title="Speed unit"
        android:summary="%s"
        android:entries="@array/speed_unit_entries"
        android:entryValues="@array/speed_unit_values"
        android:defaultValue="region" />

    <EditTextPreference
        android:key="pref_overspeed_grace_value"
        android:title="Overspeed alert grace margin"
        android:defaultValue="0" />

    <SwitchPreferenceCompat
        android:key="pref_show_battery_pct"
        android:title="Show battery percentage"
        android:summary="Display the % text next to the battery icon"
        android:defaultValue="true" />

    <SwitchPreferenceCompat
        android:key="pref_hide_status_bar"
        android:title="Hide status bar"
        android:summary="Maximize screen space on the main display"
        android:defaultValue="false" />

    <SwitchPreferenceCompat
        android:key="pref_rotate_normal"
        android:title="Rotate display 180° (normal mode)"
        android:summary="Useful for matching charge port orientation"
        android:defaultValue="false" />
    <SwitchPreferenceCompat
        android:key="pref_rotate_hud"
        android:title="Rotate display 180° (HUD mode)"
        android:summary="Useful for matching charge port orientation"
        android:defaultValue="false" />

    <ListPreference
        android:key="pref_audio_channel"
        android:title="Alert sound channel"
        android:summary="%s"
        android:entries="@array/audio_channel_entries"
        android:entryValues="@array/audio_channel_values"
        android:defaultValue="alarm" />

    <PreferenceCategory android:title="Speed limit indicators">
    <SwitchPreferenceCompat
        android:key="pref_indicator_audible"
        android:title="Audible alert"
        android:defaultValue="true" />
    <SwitchPreferenceCompat
        android:key="pref_indicator_line"
        android:title="Underline indicator"
        android:defaultValue="true" />
    <SwitchPreferenceCompat
        android:key="pref_indicator_opacity"
        android:title="Sign opacity indicator"
        android:summary="Sign becomes fully opaque when the limit is breached"
        android:defaultValue="true" />
    <SeekBarPreference
        android:key="pref_sign_base_alpha"
        android:title="Sign opacity (normal)"
        android:summary="How visible the speed-limit sign is when not breached"
        android:defaultValue="30"
        android:max="100"
        app:showSeekBarValue="true" />
    </PreferenceCategory>

</PreferenceScreen>